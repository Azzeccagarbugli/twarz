import 'package:flutter/material.dart';

// Project name
const kProjectName = 'T.W.A.R.Z.';

// Colors
const kPrimaryColor = Color(0xFFD98825);
const kSecondaryColor = Color(0xFFD93A2B);
const kThirdColor = Color(0xFFF2E9CE);

// Dark colors
const kDarkestText = Color(0xFF0D0D0D);
const kDarkText = Color(0xFF40311E);

// Background color
const kBackgroundColor = Colors.white;

// Logo density
const double kLogoSize = 160;

// Padding value
const double kPaddingValue = 16.0;

// Space text
const double kSpaceXS = 4.0;
const double kSpaceS = 8.0;
const double kSpaceM = 16.0;
const double kSpaceL = 32.0;

// Space horizontal text in Onboarding
const double kPaddingHor = 42.0;

// Border radius
final BorderRadius kBorderRadius = BorderRadius.circular(16.0);

// Welcome message
const kWelcome = '''
Tell me What Affects youR buZz
''';

// Description of emotions
const kEmotions = '''
T.W.A.R.Z. is a simple app that is able to detect and store your emotions.

Its goal is to help researchers to collect valuable data to be used in their attempt to make the world a better place!

Let's go!
''';
